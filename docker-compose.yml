version: '2.4'
services:
    serv:
        build: ./
        logging:
            options:
                max-file: 3
                max-size: 10m
        ports:
            - "5000:5000"
        environment:
            TP_PY_GRPC_SYNC_LOG_LEVEL: "INFO"